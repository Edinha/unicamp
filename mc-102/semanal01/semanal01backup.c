/*
 *     diaEvento = diaData + (diasConvertido % diasEmUmMes);
 *         mesEvento = mesData + (diasConvertido / diasEmUmMes);
 *             anoEvento = anoData;
 *                 
 *                     mesEvento = mesEvento + (diaEvento / diasEmUmMes );
 *                         anoEvento = anoEvento + (mesEvento / mesesEmUmAno );
 *                             mesEvento = mesEvento %  mesesEmUmAno;
 *                                 diaEvento = diaEvento % diasEmUmMes;  */

